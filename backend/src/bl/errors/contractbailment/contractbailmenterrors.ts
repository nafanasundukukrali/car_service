export enum ErrorExistingOfContractBailment {
    noContractBailment = "contract bailment no exist",
    contractBailmentExist = "contract bailment exist"
}

export enum ErrorInccorectPeriod {
    noCorrectDuration = "no correct duration of storage",
    noCorrectDateStart = "date less today"
}