export enum errorProductStorageInDB {
    productStorageExistsInDB = "storage product exists",
    productStorageNotInDB = "storage product not exists"
}

export enum errorArhiveNotArhive {
    isNotArchived = "object isn't achived",
    isArchived = "object is archived"
}