export enum errorServiceExists {
    serviceExists = "service actually exists",
    serviceNotExists = "srvice not exists"
}

export enum errorDirtyShedular {
    addedRecordIncorrect = "Impossible to add new shedular record, because it would break timetable"
}