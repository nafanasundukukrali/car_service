export enum errorSheduleExisting {
    recordExists = "record actually exists",
    recordNotExists = "record not exists yet"
}

export enum errorSheduleRecordDelete {
    impossibleDelete = "impossible to delete record, because existing of applications"
}

export enum errorShedularRecordTimeValidation{
    notEnoughInfo = "not enough info for time validation"
}