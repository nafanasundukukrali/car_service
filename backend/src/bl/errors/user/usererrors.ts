export enum errorDateBirth {
    less18 = "less 18", 
    dateolder = "date older than datebirth"
};

export enum errorEmail {
    inccorrect = "incorect email format"
}

export enum errorPhone {
    inccorrect = "incorect phone format"
}

export enum errorUserInDb {
    userExist = "user exist",
    userNotExist = "user not exist in db"
}

export enum errorDataAccess {
    impossibleAccess = "no access rights"
}